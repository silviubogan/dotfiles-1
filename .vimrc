" ============================================================================================
" $ cd
" $ rm .vimrc
" $ mc
"   select .vimrc from this repo, Ctrl + X, then S to have a symlink to it
"   You will update only the file in repo, restart vim and the news settings will be available.
" ============================================================================================


" Display line numbers on the left
set number
